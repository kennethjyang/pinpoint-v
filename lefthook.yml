pre-commit:
  jobs:
    - name: format
      glob: "src/*"
      run: npm run oxfmt {staged_files}
      stage_fixed: true
    - name: lint
      run: npm run lint
      stage_fixed: true
    - name: type-check
      run: npm run type-check
      stage_fixed: true
